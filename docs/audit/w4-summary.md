# W4 meta-summary — deduped, ranked findings across all four slices

Date: 2026-07-15. Inputs: `w4-ioresumesound.md` (IRS), `w4-refinement.md` (REF),
`w4-adequacy.md` (ADQ), `w4-server-codec.md` (SVC), plus the W2/W3/W4 sections of
`docs/model-strengthening-plan.md`. Cross-slice duplicates merged into one entry each;
severity/classification disagreements reconciled with the stronger slice's evidence named.
Grep-verified before ranking: `primeRootHints` exists only in `Main.lean` (no theorem);
no `GluelessProv (mkSbelt …)` lemma exists; the only `WorldModels`-concluding theorems are
the two frame transports (`WorldModels_oracle`, `WorldModelsTcp_tcpOracle`).

Severity: 5 = capstone-invalidating / real-system hole, 4 = major scope or vacuity gap,
3 = real but bounded, 2 = documented-scope or cosmetic-with-teeth, 1 = cosmetic.
Class: UR = unrealistic/undischarged premise, OS = over-scoped conclusion, U = unused
premise, DEAD = dead theorem, DS = documented scope limit.

## 1. Ranked master table

| # | Finding | Sev | Class | Slices | Files / lines | Suggested action |
|---|---------|-----|-------|--------|---------------|------------------|
| 1 | **`primeRootHints` breaks every serve capstone at the real entry point.** The cache-invariant pack (`CacheWf`…`CacheNegSoaCanon`) is established only for `DnsCache.empty` (`ServePack_empty`), but `main` serves from `primeRootHints rootServers DnsCache.empty now`, which does two `cacheUnlessTruncated` writes from a decoded-but-**unsanitized** network reply (no `sanitizeTtlsCap`). No `ServePack (primeRootHints …)` theorem exists — grep confirms `primeRootHints` appears nowhere in `VeriDNS/Proof/`. Excessive TTLs from a hostile first hop reach `storeChecked` uncapped; this is also the one cache-write site with no `RRWireCanon` discharge (W0 gap). | **5** | UR (at entry) | SVC #1 | `VeriDNS/Main.lean:37–60,140`; `VeriDNS/Proof/ServeSequence.lean:33`; `VeriDNS/Proof/DeliveredWire.lean:525` | Add `sanitizeTtlsCap` to the priming path (impl change), then prove `ServePack (primeRootHints …)` from the existing preservation lemmas — or route priming through the verified resolve path. → **W2** (plus one impl edit). |
| 2 | **`WorldModels`/`WorldModelsTcp` are never discharged for any world — satisfiability of the whole soundness spine is unpinned.** Only "producers" are the frame transports `WorldModels_oracle` / `WorldModelsTcp_tcpOracle` (grep-verified); no `⊢ WorldModels … concreteWorld` exists, not even for a one-server toy net, and the cooperative FreeIO world (`CooperativeNetworkAddr`) is a disjoint formalism. Only the vacuous `oracle = fun _ _ => none` world satisfies the pack trivially. *Reconciliation:* REF rates 5, IRS rates 4; REF's evidence is stronger — it identifies the vacuity risk explicitly (the entire conditional spine has no registered satisfiability witness) and notes `tcpSpoofReply_of_honest` already demonstrates the inhabitation recipe for one arm. Sev 5 adopted. | **5** | UR (unwitnessed TCB) | REF #1, IRS §4 | `VeriDNS/Proof/NetworkSim.lean:64–198`; `VeriDNS/Proof/IoResumeSound.lean:186,3468`; consumers in ResolveWithIOSound / ServeSequence / ServeTcp | Prove `WorldModels`/`WorldModelsTcp`/`net.WF` for one concrete honest single-server world; register as the vacuity guard. → **W2** witness lemmas. |
| 3 | **`Resolves.gaveUp` is premise-free, so every SERVFAIL "verdict" conjunct is content-free.** The constructor (`Spec/NetworkSemantics.lean:1661`) takes only `(c, slist, q)`: any query has a servFail verdict in any cache/net. The error arms of `serveDatagram_verdict_sound` (+ TCP twin), the servFail case of `ioResumeLoop_sound`'s conclusion, and the entire `ioResumeLoop_error_sound` family (whose caller dispatches on the error **string** with an unconditional `gaveUp` catch-all) deliver only invariant preservation on that path. Matches the recorded open "give-up 4". *Reconciliation:* IRS and SVC rate 4, REF rates 3 ("not unsound"); sev 4 adopted — IRS's tracing shows the *whole error family's* RFC content reduces to the unguarded rule, which is a capstone-conclusion property, not a footnote. | **4** | OS | IRS §1/§2, REF #8, SVC #3 | `VeriDNS/Spec/NetworkSemantics.lean:1661`; `VeriDNS/Proof/WorldNetwork.lean:419–441`; `VeriDNS/Proof/ResolveWithIOSound.lean:3449`; `VeriDNS/Proof/IoResumeErrorSound.lean:706` | Condition `gaveUp`/`exhausted` on a genuine failure witness (slist exhausted / deadline) in the model; until then annotate the capstones that the error arm is invariants-only (adequacy/no-starvation covers the non-trivial direction). → **W2** (model rule) + **W3** (annotation). |
| 4 | **`WorldModelsTcp` has no adversarial arm — an on-path/MITM TCP attacker is outside every capstone.** `WorldModelsTcp` (NetworkSim.lean:151) requires every accepted TCP reply to be honest (`serverAt`+`ServerAnswers`+`linkReach`); the UDP pack has the `SpoofReply` arm, TCP does not. Documented only in `docs/tcp-plan.md` decision 5 (off-path rationale); invisible at the theorem statements. `tcpSpoofReply_of_honest` exists precisely because of this asymmetry. | **4** | UR (honest-only oracle) | IRS §1/§3; SVC #13 (honest-arm note) | `VeriDNS/Proof/NetworkSim.lean:151`; `VeriDNS/Proof/IoResumeSound.lean:3440`; `docs/tcp-plan.md:74–76` | Either add a 4-tuple-constrained adversarial TCP arm (much weaker than UDP spoof) or surface the on-path exclusion as a named assumption in `docs/architecture.md` + capstone docstrings. → **W2**. |
| 5 | **The impl serves ANY/non-IN queries with zero coverage, and one such datagram voids the whole trace theorem.** `queryProblem` (Impl/Server.lean:118) does not reject qtype=ANY or non-IN class, so the running server answers `dig ANY` / `dig CH TXT` unverified; `InScope` (ServeSequence.lean:53) quantifies the gate over the whole trace, so one out-of-scope datagram voids `serveSeq_total` for all surrounding IN/A traffic. *Reconciliation:* IRS and REF file the qtype/class gates as DS sev 2 (recorded T5 scope keeps); SVC's evidence is stronger because it found two **new** aggravations (impl serves them anyway; trace-voiding `InScope`) beyond the recorded scope keep. Sev 4 for those two; the underlying scope keep stays DS-2. | **4** | UR / DS | SVC #2; IRS (DS rows), REF #11 | `VeriDNS/Impl/Server.lean:118`; `VeriDNS/Proof/ServeSequence.lean:53` | Short term: refactor `serveSeq_*` so out-of-scope datagrams degrade to invariant-preservation-only. Long term: extend the model to `QType.star`/non-IN, or make `queryProblem` refuse them (turning the gate into a derivable fact). → **W3** (generalisation) + one impl decision. |
| 6 | **Serve capstones never pin the bytes actually sent, and `frameTcp` has a >65535 framing hole.** `sendTo`/`tcpSend` are `.pure ()` in Prog, so the "delivered" object is the internal `deliveredResponse`; the wire conjunct is gated on `encode … ≤ clientCap` (UDP) / `≤ 65535` (TCP) and nothing equates the conclusion with the datagram passed to `sendTo` (the proof obtains `hrunSend` and discards it). TCP: `serveTcpDatagram` sends `frameTcp (encode response)` with **no size guard**; `frameTcp` writes a mod-truncated 2-byte length for oversize payloads — corrupt framing exactly where the theorem's gate excludes. | **4** | OS (+1 impl hole) | SVC #4 | `VeriDNS/Proof/FreeIO.lean:49,54`; `VeriDNS/Impl/Server.lean:802`; `VeriDNS/Impl/TcpFraming.lean`; `VeriDNS/Proof/Server.lean:53,91` | Export `sent = truncateUdp …` / `sent = frameTcp …` as conclusion conjuncts (proof already has them). Add the oversize/TC=1 arm. Guard `frameTcp` at 65535 or prove the bound unreachable. → **W3** + one impl edit. |
| 7 | **Provable facts are baked into the `WorldModels`/`SpoofReply` axiom, inflating the TCB.** The per-RR conjuncts (`parseRaw b = some rr ∧ αRR rr ≠ none`, NS-rdata canonicity) mention neither `net` nor `ns` — they are facts about the impl-decoded `resp`, derivable from `decodeRRCanonical` + `sanitizeTtlsCap`, yet are assumed inside the trusted network pack. IRS adds the dual risk: any SpoofReply conjunct NOT implied by acceptance narrows the adversary unrealistically (recorded spoofed-referral n-canonicity boundary). | **4** | UR (assumed where provable) | REF #2, IRS §4 | `VeriDNS/Proof/NetworkSim.lean:64–198`; `VeriDNS/Impl/Message.lean:12,70` | Prove `decode ok → sanitize ok → (parse/αRR/canonicity)` as standalone lemmas; delete those conjuncts from the pack and discharge at consumers; per-conjunct derivability audit of the SpoofReply referral block. → **W2** (overlaps W0). |
| 8 | **Single-NS `SlistShape` — adequacy holds on a strictly narrower delegation shape than soundness.** Four coupled collapse points (section singletons `hauthN/haddN = #[…]`; `walkNs` collapse; `reGlue` collapse; `SlistShape` itself, SpineAdequacy.lean:158) gate the whole spine/depth1/flat adequacy families to one-NS, one-glue, fully-glued cuts. *Reconciliation:* ADQ rates 4 with a full generalisation plan; REF files it DS-2 (recorded residual). ADQ's characterisation is stronger (it identifies the exact entry points and shows `depth1Delegation_chain`/`bestWithAddress_mem` are already multi-glue-ready, so the collapse is caller-introduced); sev 4 adopted as the top W3 structural item. | **4** | UR (shape gap) | ADQ §gap, REF #19 | `VeriDNS/Proof/SpineAdequacy.lean:158` + families listed in ADQ's gap section; `VeriDNS/Proof/Depth1Adequacy.lean:64,188`; `VeriDNS/Proof/CooperativeNetwork.lean:1906` | Follow ADQ's dependency-ordered plan (`SlistShape'` over an `nsSet`, per-element write inverses, array-valued `HopOk`, set-carrying `SpineOk`). → **W3**. |
| 9 | **`gluelessNs_anchor_witness` fabricates the cprov cache — glueless verdicts are not anchored to the resolver's actual cache.** Zero hypotheses: for ANY `nsQ`/`qn` it manufactures `cprov = {pos := [root NS nsQ]}` satisfying the model's glueless connector, so the `HasVerdictAt` justification for glueless descent never certifies cache provenance. Recorded fork-(b), but the induced weakening is undocumented at the capstone. | **4** | OS | IRS §1 | `VeriDNS/Proof/IoResumeSound.lean:2328` | Strengthen the model glueless rule to tie `cprov` to the abstracted resolver cache (the impl-side facts already flow through `hGlProv`). → **W2**. |
| 10 | **`NetworkConsistent`/`NetworkConsistentTcp` are never discharged — the completeness capstones (incl. an `rfc_proves [2181]` citation) rest on an unwitnessed honest-network premise.** At `M := Prog`, `SatisfiesM` over `forwardQuery` asserts EVERY possible oracle result is tree-consistent; no real or mock world realizes it, and the actually-discharged cooperative track (SpineAdequacy) bypasses these theorems. *Reconciliation:* REF 4 vs ADQ 3 — REF's evidence is stronger on impact (the RFC citation rests on it; `NameTree.resolveWithIO_sound` also has zero callers), ADQ contributes the fix (`NetworkConsistent T Prog Unit` for `mkHonestOracle (treeRespond T neg)` looks provable from `treeRespond_*`). Sev 4. | **4** | UR | REF #4, ADQ #5 | `VeriDNS/Proof/NameTree.lean:1692,1822,2010`; `VeriDNS/Proof/NameTreeComplete.lean:3230,3483` | Prove the `mkHonestOracle`/`treeRespond` instance to connect the completeness chain to the cooperative worlds; document the honest-network scope meanwhile. → **W2**. |
| 11 | **`resolveThenReply_sound` is dead AND undischargeable.** Zero callers; its combined `SatisfiesM (ShimSound ∧ CanonicalSection)` premise has no producer and cannot be built (no generic `SatisfiesM.and`, recorded lesson); the two halves live in incompatible formalisms. The ServeSound layer is superseded by `serveDatagram_verdict_sound`. *Reconciliation:* REF 4 vs SVC 3 — SVC's calibration adopted (sev 3: dead code weakens no live guarantee; the harm is a soundness-looking vacuous theorem), REF's formalism-mismatch analysis is the stronger evidence for *why* it cannot be repaired in place. | 3 | UR + DEAD | REF #3, SVC #7 | `VeriDNS/Proof/ServeSound.lean:108` (+ supplier chain :83) | Retirement candidate (see §2.4 — user sign-off required); alternative is restating the shim chain at `M := Prog`. |
| 12 | **`resolveWithIO_simulates` assumes its own conclusion and is dead.** The 4th `houtcome` disjunct is literally the theorem's conclusion (network arm assumed, not proven); zero callers — the live spine is `resolveWithIO_verdict_sound`. Its only-caller status also strands `resolveWithIO_cacheHit_simulates` / `negHit_nodata_simulates` (fine theorems, dead consumer). | 3 (was REF 4; demoted with #11's dead-code calibration) | OS + DEAD | REF #5, #21, #22 | `VeriDNS/Proof/Refinement.lean:8863,8055,8008` | Retirement candidate (§2.4); if kept, rename `resolveWithIO_cacheArms_simulate` and drop the conclusion-shaped disjunct. |
| 13 | **`hglueless` is dead plumbing through the whole delivery chain (~16 signatures).** `run_ioResumeLoop_answer`/`_nxdomain` take `slist.addressTargets[0]? = none` but never use it (the loop reads `addressTargets` only under `bestWithAddress = none`, which `hbest = some _` forecloses); verified line-by-line in both slices; `lean_minimal_hypotheses` "load-bearing" verdict shown to be call-site arity artifacts. Re-demanded unused by ~12 downstream signatures; `spineDelegation_chain` manufactures it just to feed it. Deleting it also loosens one binding to the single-NS shape (#8). | 3 | U | REF #7, ADQ #1 | `VeriDNS/Proof/FreeIO.lean:275,403`; CooperativeNetwork + SpineAdequacy call sites | Delete bottom-up (checklist §2.3). Free strengthening. |
| 14 | **Cold-cache premises linger below the warm spine capstone.** `referralWrite_reGlue_exact` (warm twin exists, unused by depth1), `resolveWithIO_depth1_adequate`, `serveDatagram_depth1_adequate` all still take `hcold : c.records = #[]`; notably the spine **corollaries** `resolveWithIO_within_bound` and `resolveWithIO_spine_no_starvation` are stated over the cold capstone though `_warm` exists — the fuel bound and no-starvation guarantees are silently cold-only. | 3 | UR | ADQ #3 | `VeriDNS/Proof/Depth1Adequacy.lean:64,188`; `VeriDNS/Proof/ServeAdequacy.lean:126`; `VeriDNS/Proof/SpineAdequacy.lean` (corollaries) | Restate corollaries over `_warm` (mechanical); switch depth1 to `referralWrite_reGlue_exact_warm` + `DescentGlueInv.aNone`. → **W3**. |
| 15 | **Serve-boundary adequacy conclusions don't pin the client payload.** `serveDatagram_delivers_of_resolve`, TCP sibling, and the serve conjunct of `serveDatagram_depth1_adequate` conclude only termination with *some* cache/world; the answer-content pin lives at the resolver boundary, before scrub/truncate/encode. "Delivers" over-promises. No `serveTcpDatagram_depth1_adequate` exists at all. | 3 | OS | ADQ #4 | `VeriDNS/Proof/ServeAdequacy.lean` | Pin `replyForResolution`'s output (post-scrub bytes) in the conclusions; add the TCP depth1 twin. Dual of finding #6. → **W3**. |
| 16 | **`serveSeq_total` is unconnected to the deployed loop.** No consumers outside ServeSequence.lean; the deployed loops differ structurally: periodic `sweep` absent from `serveSeq`; concurrent UDP+TCP sharing one mutex cache unmodeled (no TCP trace combinator at all); `w'.clock = w.clock` freezes the trace at one instant (no cross-datagram TTL expiry). | 3 | OS | SVC #6 | `VeriDNS/Proof/ServeSequence.lean:215,333`; `VeriDNS/Main.lean:85–128`; `VeriDNS/Impl/Server.lean:845` | TCP-inclusive trace combinator; include the sweep step (preservation lemmas exist); document the frozen-clock scope. → **W3**. |
| 17 | **`networkAnswer_simulates`/`networkNxdomain_simulates` are dead, empty-cache-scoped, and assume their payload facts.** Verdict lives on a synthetic one-server net built from the impl's own output over `Cache.empty`; `hrun` restated verbatim as a conclusion conjunct; zero callers (superseded by the IoResumeSound spine). | 3 | OS + DEAD | REF #6 | `VeriDNS/Proof/NetworkSim.lean:16,42`; `VeriDNS/Proof/WorldNetwork.lean` (`answerNet` scaffolding) | Retirement candidates (§2.4) together with `answer/nxdomain/nodata_model_realizable` + `answerNet`/`answerRespOf` (keep the live `*_hasVerdictAt` error-terminal lemmas). |
| 18 | **`RRsetComplete` is fresh-key-only and orphaned.** `storeStep_foldl_survivor`/`cacheRRsNorm_complete` require the key absent from the cache — excluding RRset refresh, the warm common case and exactly where the `Blocked` gate can drop members (bugs.md #4). `cacheRRsNorm_complete` has zero consumers and no `rfc_proves` citation. | 3 | UR + OS | ADQ #6 | `VeriDNS/Proof/RRsetComplete.lean` | Blocked-aware survivor statement or the recorded coordinated model+impl TTL-normalisation; wire into a capstone or retire (§2.4). → **W3** / bugs.md #4. |
| 19 | **`GluelessProv (mkSbelt rootServers)` unproven — the premise every capstone consumes is never established for the deployed SBELT.** Instances exist for `default`/`fromNsWithGlueAll`/`markQueried`/`addAddress`/`removeServer` but not `mkSbelt` (grep-verified). Root-hint names are fixed canonical literals, so the lemma is `decide`-adjacent. *Reconciliation:* IRS 4 vs SVC 3 — SVC's 3 adopted: severity should reflect residual risk given near-certain dischargeability; IRS's 4 conflated impact with the (real) end-to-end hole. Top quick win. | 3 | UR (provable) | IRS §1, SVC #5 | `VeriDNS/Main.lean:13,136`; `VeriDNS/Proof/IoResumeSound.lean:2244–2267` | Add `GluelessProv_mkSbelt` (or the concrete pin) and cite next to `main`. → **W2** witness lemma; quick win. |
| 20 | **`net.WF` assumed at every level, never discharged for a concrete net.** Part of the same boundary as #2; falls out of the witness world. | 3 | UR (boundary) | IRS §1 | consumers up to `serveSeq_total` | Discharge together with the #2 witness. → **W2**. |
| 21 | **`ioResumeLoop_error_sound` / `resolveWithIO_error_sound` names over-promise.** Statements deliver cache-invariant preservation only; the "verdict" is manufactured by the caller via the unconditional give-up rules (#3). | 2 | OS (naming) | IRS §2, REF #25 | `VeriDNS/Proof/IoResumeErrorSound.lean:706`; `VeriDNS/Proof/ResolveWithIOSound.lean:3449` | Rename `*_error_invariants`, or fold in a real verdict once #3's guard lands. |
| 22 | **Honest arm = "honest AND fits the negotiated cap".** An honest-but-truncated UDP reply can only inhabit `SpoofReply` (zero honest guarantees). Deliberate and sound (TC → TCP), but undocumented at the pack. | 2 | DS (note) | REF #9, SVC #13a | `VeriDNS/Proof/NetworkSim.lean:112,147` | Docstring note; diff rig should exercise the TC=1 honest case. → **W5**. |
| 23 | **Probe-ladder adequacy assumes RFC-8020-conformant parents** (`plk`: all intermediate probes `.nodata`; NXDOMAIN-on-ENT parents outside adequacy while soundness covers them); **full-glue assumed** (`hall`, partially-glued cuts route to the unexercised glueless branch); **whole-oracle cooperativity** (one timeout then honest retry outside every adequacy theorem). | 2 | UR (mild) | ADQ (HopOk `plk`, `bestWithAddress_reGlue_resolves`, CooperativeNetwork note) | `VeriDNS/Proof/SpineAdequacy.lean` (HopOk), `VeriDNS/Proof/CooperativeNetwork.lean` | Denial-tolerant probe round; glueless adequacy dual; `HonestAt w addr respond` refactor. → **W3** backlog. |
| 24 | **`qm.rd = false` model-query pin is a reader trap** (impl *requires* RD=1; model rd-irrelevant). Sev per SVC (2, notes the impl requires RD=1 making the mismatch sharper) over IRS/REF (1). | 2 | DS (cosmetic) | IRS §1, REF #13, SVC #10 | capstone statements; `VeriDNS/Spec/NetworkSemantics.lean:1787` | One comment citing `resolves_rd_irrelevant` (or set `rd := true`). Quick win. |
| 25 | **Minor unused/derivable premises:** `hqsf` derivable from `hrcq` (5 spine signatures); `hqstar` derivable from `hqq` (~5 sites); `resolves_delivered_no_foreign`'s underscore-bound `Resolves` premise; `sent`/`hsentEq` alias in depth1; redundant `hα`+`hcan` canonicity pairs (4 lemmas); seed-pinned `hlk` in depth1 (prefer ∀-seed). | 1–2 | U / D | ADQ #7, REF #12, SVC #8, IRS §5 | see §2.3 | Deletion checklist §2.3. |
| 26 | **Dead minor declarations:** `resolveWithIO_adequate_of_descent`, `cooperativeReferral_continue_terminalFacts` (non-`_exact`), the `WfTree` cluster (`treeLookup_answer_wfTree` 0 refs), `NameTree.resolveWithIO_sound` (0 callers). | 1–2 | DEAD | ADQ, REF | `VeriDNS/Proof/Adequacy.lean`, `CooperativeNetwork.lean`, `NameTree.lean:2010` | Retirement candidates (§2.4). |

Note on the one apparent factual conflict: IRS reports "Unused: 0" for its slice while REF/ADQ
report `hglueless` unused — no contradiction; `hglueless` lives in FreeIO/CooperativeNetwork/
SpineAdequacy, outside IRS's slice. Similarly SVC's `hrd` finding (impl requires RD=1) refines,
not contradicts, IRS/REF's "definitional selector" reading.

## 2. Routing to plan workstreams

### 2.1 Assumed-oracle premises → W2 obligations

Concrete artifacts to create (ModelInvariant fields / witness lemmas / model-rule changes):

1. **Witness world** (findings #2, #20): `WorldModels_witness : WorldModels net₀ ns₀ ra b now (worldOf net₀)` and `WorldModelsTcp_witness` for one concrete honest single-server net, plus `netWF_singleServer : net₀.WF`. Recipe: `tcpSpoofReply_of_honest` already inhabits one arm. Register as the vacuity guard; pair each difftest scenario with a WF net.
2. **TCP adversary** (finding #4): either a new `TcpSpoofReply` disjunct in `WorldModelsTcp` (attacker constrained by the connection 4-tuple) or a named `OnPathTcpExcluded` assumption surfaced in `docs/architecture.md` + the capstone docstrings. Decision needed; the disjunct is the model-strengthening option.
3. **Guarded give-up** (finding #3): condition `Resolves.gaveUp` (and `exhausted`) on a failure witness — slist exhausted / deadline passed — as a model-rule change; the fuelRank machinery (`resume_ne_maxIterations`) is the impl-side fact to mirror. A `ModelInvariant` field `failureJustified` making every servFail verdict carry the witness.
4. **Shrink the world axiom** (finding #7): lemmas `decodeRRCanonical_parseRaw_total`, `decodeRRCanonical_αRR_isSome`, `decodeRRCanonical_ns_rdata_canonical` (from `decode = .ok` + `sanitizeTtlsCap`), then delete the corresponding conjuncts from `WorldModels`/`SpoofReply` and discharge at consumers. Companion: per-conjunct derivability audit of the SpoofReply referral block (the recorded trailing-bytes boundary becomes a named obligation where derivation fails). Overlaps W0's `CanonicalRaw`.
5. **Completeness-chain oracle** (finding #10): instance `NetworkConsistent T Prog Unit` (and Tcp twin) for `w.oracle = mkHonestOracle (treeRespond T neg)`, provable from the `treeRespond_*` lemma family; reconnects `resolveWithIO_complete`'s `rfc_proves [2181]` citation to a realizable world.
6. **Priming pack** (finding #1): `ServePack_primeRootHints : ServePack (primeRootHints roots DnsCache.empty now) …` — after adding `sanitizeTtlsCap` to the priming path (impl edit). The same theorem discharges the W0 `cacheRecCanon_cacheRRs` store premise at the one uncovered write site.
7. **SBELT witness** (finding #19): `GluelessProv_mkSbelt` (or concrete `GluelessProv (DnsSList.mkSbelt rootServers)`), cited next to `main`.
8. **cprov anchoring** (finding #9): strengthen the model glueless rule so `cprov` is tied to the abstracted resolver cache; retire `gluelessNs_anchor_witness`'s hypothesis-free fabrication. Impl-side facts already flow through `hGlProv`.
9. **Documentation-only W2 items**: honest-arm-fits-cap note (finding #22, + W5 rig case); frozen-clock scope of `serveSeq` (finding #16); qtype/class scope statement in `docs/architecture.md` next to the capstones (finding #5's DS residue).

### 2.2 Over-scoped conclusions → W3 duals / generalisations

Merged with ADQ's missing-duals table; suggested priority order (ADQ's, extended):

1. **NXDOMAIN at general depth** (`.nameError` leaf alternative in `LeafOk`; `treeRespond_nxdomain_*` exist).
2. **Negative cache-hit adequacy** (mirror `run_resolveWithIO_answerHit`; `.negative` arm + `lookupNegative_verdict` exist).
3. **Client-payload pin at the serve boundary** (findings #6, #15): export the `sent =` conjuncts in `serveDatagram_verdict_sound`/TCP twin; pin `replyForResolution` output in the `*_delivers_of_resolve` conclusions; add `serveTcpDatagram_depth1_adequate`; add the oversize/TC=1 arm; `frameTcp` 65535 guard (impl).
4. **Multi-NS `SlistShape` generalisation** (finding #8): follow ADQ's dependency-ordered plan verbatim (`SlistShape'`, per-element inverses, array-valued `HopOk`, set-carrying `SpineOk`); prerequisite eased by the `hglueless` deletion (§2.3).
5. **Warm restatement** of `resolveWithIO_within_bound` / `resolveWithIO_spine_no_starvation` + warm depth1 (finding #14) — mechanical.
6. **Out-of-scope datagram degradation** in `serveSeq_*` (finding #5): per-datagram case split instead of trace-voiding `InScope`.
7. **Authority-section model agreement** in the verdict capstones (SVC #9): tie delivered SOA on negative answers to `v.authority`.
8. **serveSeq → deployed loop** (finding #16): sweep step + TCP trace combinator + concurrency scope note.
9. Backlog (finding #23 + ADQ's table): NODATA terminal; CNAME-chase adequacy (`cnameChain = #[]` currently required everywhere); glueless-arm adequacy (`bestWithAddress = none` unexercised); TC→TCP fallback dual; probe-denial tolerance; per-address `HonestAt` cooperativity; scrub-preserves-answer adequacy for `replyForResolution`.
10. **Give-up annotation** (finding #3's W3 half) and `*_error_sound` renames (finding #21).
11. `dual_of` registration (plan W3) for every pair above as it lands.

### 2.3 Unused premises → deletion checklist (bottom-up by dependency)

All deletions are free strengthenings; each step is edit-binder-then-fix-callers.

1. `resolves_delivered_no_foreign` — drop the underscore-bound `_h : Resolves` premise (`VeriDNS/Proof/DeliveredAuthentic.lean:26`). Leaf; no cascade. (Or retire, §2.4.)
2. `hglueless` roots: `FreeIO.run_ioResumeLoop_answer` (:275), `FreeIO.run_ioResumeLoop_nxdomain` (:403) — delete the binder (and `hglueless₂` where present).
3. `hglueless` FreeIO re-exports: `run_resolveWithIO_networkAnswer` + siblings (FreeIO:398, :1588 call sites).
4. `hglueless` CooperativeNetwork layer: `honestAnswerRound_delivers`, `honestNxdomainRound_delivers`, `flatAuthoritative_{answer,nxdomain}Round_delivers`, `delegating{Answer,Nxdomain}Round_delivers`.
5. `hglueless` top layer: `treeAnswerRound_delivers_pinned`, `resolveWithIO_flatAuthoritative_{answer,nxdomain}_adequate`, and remove `spineDelegation_chain`'s `SlistShape.addressTargets_none` manufacture.
6. `hqsf` (derivable from `hrcq` via `rw [hrcq]; decide`): `spineDelegation_chain`, `resolveWithIO_spine_adequate_warm`, `_adequate`, `_within_bound`, `_no_starvation` — derive internally, drop binder top-down within this group (each consumes its own).
7. `hqstar` (derivable from `hqq` via `noConfusion`): `resolveWithIO_verdict_sound`, `resolveWithIO_paused_full_sound`, `resolveWithIO_paused_sound(')` — inline at consumers.
8. Cosmetic, do only if touching the files anyway: `sent`/`hsentEq` inlining in `resolveWithIO_depth1_adequate`; the redundant `hα`+`hcan` canonicity pairs (IRS §5, needs the stronger validity form — defer); `hqu` → `∃ qu` restatement (SVC #11, optional); seed-pinned `hlk` → ∀-seed form in depth1.

### 2.4 Dead theorems → retirement list (REQUIRES USER SIGN-OFF — candidates, not decisions)

Each verified zero-callers by grep in at least one slice. Options per item: delete, demote to
comment/example, or rewire a consumer. None of these is decided.

| Candidate | Location | Why | Alternative to deletion |
|---|---|---|---|
| `resolveWithIO_simulates` | Refinement.lean:8863 | assumes its own network disjunct; 0 callers; live spine is `resolveWithIO_verdict_sound` | rename + drop the conclusion-shaped disjunct; keeps `cacheHit`/`negHit_nodata_simulates` alive |
| `networkAnswer_simulates`, `networkNxdomain_simulates` + `answerNet`/`answerRespOf`/`answer/nxdomain/nodata_model_realizable` scaffolding | NetworkSim.lean:16,42; WorldNetwork.lean | synthetic self-referential net over `Cache.empty`; 0 callers; superseded by IoResumeSound spine. Keep the live `*_hasVerdictAt` error-terminal lemmas. | none plausible |
| `resolveThenReply_sound` (+ decide the ServeSound layer's fate) | ServeSound.lean:108 | 0 callers AND undischargeable premise | restate shim chain at `M := Prog` and wire a consumer |
| `NameTree.resolveWithIO_sound` | NameTree.lean:2010 | 0 callers; gated on undischarged `NetworkConsistent` | comes alive if §2.1 item 5 lands — defer decision until then |
| `cacheRRsNorm_complete` (and the fresh-key survivor's status) | RRsetComplete.lean | 0 consumers, no rfc citation, name over-promises | wire into a delivery-capstone answer-write conjunct, or rename `*_firstWrite` |
| `resolveWithIO_adequate_of_descent` | Adequacy.lean | 0 refs; spine uses `of_chain` | keep as documented API with an intent comment |
| `cooperativeReferral_continue_terminalFacts` (non-`_exact`) | CooperativeNetwork.lean | superseded by `_exact` (used at :3013) | none needed |
| `WfTree` cluster (`InZone`, `WfTree`, `findChild_mem`, `nodeAt_inTree`, `nodeAtName_inTree`, `lookupAt_answer_mem`, `treeLookup_answer_node`, `treeLookup_answer_wfTree`) | CooperativeNetwork.lean | purpose (whole-tree → per-answer `WfTreeRR`) superseded by pointwise `hwfRR` premises | **or invert**: wire `WfTree T` in as the nicer single premise replacing `hwfRR` at every capstone (ADQ's suggestion — arguably the better outcome) |
| `resolves_delivered_no_foreign` | DeliveredAuthentic.lean:26 | proof is exactly `scrubAnswer_no_foreign`; premise decorative | keep with the premise deleted + rename |

### 2.5 Impl edits surfaced by the audit (not W-routed, flag separately)

- `primeRootHints`: add `sanitizeTtlsCap` (finding #1) — the only audit item that is plausibly a
  live runtime weakness, not just a proof gap.
- `frameTcp`: 65535 guard or proven-unreachable bound (finding #6).
- `queryProblem`: decide whether to refuse ANY/non-IN (finding #5 long-term option).

## 3. Quick wins (each < 1 day)

1. **`GluelessProv_mkSbelt`** (finding #19) — root-hint names are fixed canonical literals; `decide`-adjacent; closes the last unproven premise of the end-to-end serve chain.
2. **`hqsf` derivations** (5 signatures, one `rw [hrcq]; decide` each) + **`hqstar`** inlining.
3. **`hglueless` deletion cascade** (~16 sites, mechanical binder+call-site edits; both slices agree the proofs never use it).
4. **Drop `_h` from `resolves_delivered_no_foreign`** (one binder).
5. **Export the `sent = truncateUdp …` conjunct** in `serveDatagram_verdict_sound` (SVC: "cheap, the proof already has it"; the TCP guard is separate).
6. **Warm restatement of `resolveWithIO_within_bound` / `_spine_no_starvation`** over `_warm` (ADQ: mechanical).
7. **Switch depth1 to `referralWrite_reGlue_exact_warm`** (`hAnone` + `DescentGlueInv.aNone` already exist).
8. **Documentation batch**: `hrd`/rd-irrelevance comment at the capstones; `WorldModelsTcp` on-path exclusion note in `docs/architecture.md` + docstring; honest-arm-fits-cap docstring; qtype/class scope statement next to the serve capstones; 2106 clock-horizon note; `Resolves.gaveUp` triviality annotation on the error arms (stopgap for finding #3).
9. **`sanitizeTtlsCap` in `primeRootHints`** — the impl half of finding #1 is a two-line edit and independently valuable even before `ServePack_primeRootHints` is proven.

The non-quick-win core, in suggested order: witness world (#2) → gaveUp guard (#3) → priming
pack proof (#1's theorem half) → world-axiom shrink (#7) → TCP adversary decision (#4) →
NXDOMAIN/negative-hit duals → multi-NS SlistShape (#8).
