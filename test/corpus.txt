# Differential-test query corpus for veri-dns vs unbound.
# Format: one query per line, "<name> <type>". Blank lines and #-comments ignored.
# Each remediation finding that has a live-network repro adds a line here.
# Injection-based findings (036/004/012/013/009b/037) are exercised by
# mock-authoritative scenarios, not this corpus. The TCP upstream TC→TCP
# fallback (stage U) has its own hermetic rig: test/tcp_difftest.sh + mock_auth.py.

# --- baseline sanity: names that must resolve identically to unbound ---
example.com A
example.com AAAA
example.org A
iana.org A
www.iana.org A
one.one.one.one A
dns.google A

# --- delegation / referral chains ---
a.gtld-servers.net A
www.ietf.org A

# --- negative / non-existent ---
nonexistent-veridns-test.example A
example.com TXT

# --- oversized answer 512B < size ≤ 1232B: must resolve over UDP end-to-end via EDNS0
#     (RFC 6891, docs/tcp-plan.md stage E). Before EDNS0 this truncated (TC=1) and dig's TCP
#     retry hit our (unsupported) TCP path — the old "TC→TCP fallback expected" rig wart. With
#     the 1232 buffer advertised upstream and honored downstream it comes back full, no TC.
#     (Answers >1232B — e.g. cloudflare.com/apple.com TXT — still truncate and need real TCP,
#     tcp-plan stages U/S; those stay TC=1 and are skipped by the harness, not asserted here.)
google.com TXT

# --- root-descending (015 SBELT-fallback regression guard) ---
. NS
